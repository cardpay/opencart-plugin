{{ header }}{{ column_left }}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="pull-right">
                <a id="btn_save" class="btn btn-primary">{{ button_save }} <em class="fa fa-save"></em></a>
                <a onclick="location = '{{ cancel }}';" class="btn btn-default">{{ button_cancel }} <em class="fa fa-reply"></em></a>
            </div>
            <h1>{{ heading_title }}</h1>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <div class="container-fluid">
        {% if error_warning %}
            <div class="alert alert-danger"><em class="fa fa-exclamation-circle"></em> {{ error_warning }}
                <button type="button" class="close" data-dismiss="alert">&times;</button>
            </div>
        {% endif %}
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><em class="fa fa-pencil"></em> Edit Unlimint</h3>
            </div>
            <div class="panel-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form_ul"
                      class="form-horizontal">
                    <ul class="nav nav-tabs" id="tabs">
                        <li class="active"><a href="#tab_settings" data-toggle="tab">{{ tab_setting }}</a></li>
                        <li><a href="#tab_order_status" data-toggle="tab">{{ tab_order_status }}</a></li>
                    </ul>
                    <div class="tab-content" id="form-table">
                        <div class="tab-pane active" id="tab_settings">
                            <div class="form-group required" id="div_payment_ul_card_status">
                                <label class="col-sm-2 control-label" for="payment_ul_card_status"
                                       data-toggle="tooltip" data-trigger="click"
                                       title='{{ entry_access_token_tooltip }}'><span
                                            data-toggle="tooltip"
                                            title='{{ help_status }}'>{{ entry_payment_status }}</span></label>
                                <div class="col-sm-10">
                                    <select name="payment_ul_card_status"
                                            id="payment_ul_card_status"
                                            class="form-control">
                                        {% if payment_ul_card_status %}
                                            <option value="1" selected="selected">{{ text_yes }}</option>
                                            <option value="0">{{ text_no }}</option>
                                        {% else %}
                                            <option value="1">{{ text_yes }}</option>
                                            <option value="0" selected="selected">{{ text_no }}</option>
                                        {% endif %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group required error inline" id="div_payment_ul_card_terminal_code">
                                <label class="col-sm-2 control-label" for="payment_ul_card_terminal_code"
                                       data-toggle="tooltip">{{ entry_terminal_code }}</label>
                                <div class="col-sm-10">
                                    <input type="text" class="form-control" id="payment_ul_card_terminal_code"
                                           name="payment_ul_card_terminal_code"
                                           value="{{ payment_ul_card_terminal_code }}"/>
                                </div>
                            </div>

                            <div class="form-group required" id="div_ul_card_terminal_password">
                                <label class="col-sm-2 control-label" for="input-settlement-immediate">
							<span data-toggle="tooltip"
                                  title='{{ help_terminal_password }}'>{{ entry_terminal_password }}</span></label>
                                <div class="col-sm-10">
                                    <input type="password" class="form-control" id="payment_ul_card_terminal_password"
                                           name="payment_ul_card_terminal_password"
                                           value="{{ payment_ul_card_terminal_password }}"/>
                                </div>
                            </div>

                            <div class="form-group required" id="div_payment_ul_card_callback_secret">
                                <label class="col-sm-2 control-label"
                                       for="payment_ul_card_callback_secret">{{ entry_callback_secret }}</label>
                                <div class="col-sm-10">
                                    <input type="password" class="form-control" id="payment_ul_card_callback_secret"
                                           name="payment_ul_card_callback_secret"
                                           value="{{ payment_ul_card_callback_secret }}"/>
                                </div>
                            </div>

                            <div class="form-group required" id="div_payment_ul_card_test_environment">
                                <label class="col-sm-2 control-label" for="input-settlement-immediate">
							<span data-toggle="tooltip"
                                  title='{{ help_test_environment }}'>{{ entry_test_environment }}</span></label>
                                <div class="col-sm-10">
                                    <select name="payment_ul_card_test_environment"
                                            id="payment_ul_card_test_environment"
                                            class="form-control">
                                        {% if payment_ul_card_test_environment %}
                                            <option value="1" selected="selected">{{ text_no }}</option>
                                            <option value="0">{{ text_yes }}</option>
                                        {% else %}
                                            <option value="1">{{ text_no }}</option>
                                            <option value="0" selected="selected">{{ text_yes }}</option>
                                        {% endif %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group required" id="div_payment_ul_card_capture_payment">
                                <label class="col-sm-2 control-label" for="input-settlement-immediate">
							<span data-toggle="tooltip"
                                  title='{{ help_capture_payment }}'>{{ entry_capture_payment }}</span></label>
                                <div class="col-sm-10">
                                    <select name="payment_ul_card_capture_payment"
                                            id="payment_ul_card_capture_payment"
                                            class="form-control">
                                        {% if payment_ul_card_capture_payment %}
                                            <option value="1" selected="selected">{{ text_yes }}</option>
                                            <option value="0">{{ text_no }}</option>
                                        {% else %}
                                            <option value="0" selected="selected">{{ text_no }}</option>
                                            <option value="1">{{ text_yes }}</option>
                                        {% endif %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group required" id="div_payment_ul_card_installment_enabled">
                                <label class="col-sm-2 control-label"
                                       for="payment_ul_card_installment_enabled"><span
                                            data-toggle="tooltip"
                                            title='{{ help_installment_enabled }}'>{{ entry_installment_enabled }}</span></label>
                                <div class="col-sm-10">
                                    <select name="payment_ul_card_installment_enabled"
                                            id="payment_ul_card_installment_enabled"
                                            class="form-control">
                                        {% if payment_ul_card_installment_enabled %}
                                            <option value="1" selected="selected">{{ text_yes }}</option>
                                            <option value="0">{{ text_no }}</option>
                                        {% else %}
                                            <option value="1">{{ text_yes }}</option>
                                            <option value="0" selected="selected">{{ text_no }}</option>
                                        {% endif %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group required" id="div_payment_ul_card_payment_title">
                                <label class="col-sm-2 control-label"
                                       for="payment_ul_card_payment_title">{{ entry_payment_title }}</label>
                                <div class="col-sm-10">
                                    <input type="text" class="form-control" id="payment_ul_card_payment_title"
                                           name="payment_ul_card_payment_title"
                                           value="{{ payment_ul_card_payment_title }}"/>
                                </div>
                            </div>

                            <div class="form-group required">
                                <label class="col-sm-2 control-label"
                                       for="payment_ul_card_entry_ask_cpf">{{ entry_ask_cpf }}</label>
                                <div class="col-sm-10">
                                    <select name="payment_ul_card_ask_cpf" id="payment_ul_card_ask_cpf"
                                            class="form-control">
                                        {% if payment_ul_card_ask_cpf %}
                                            <option value="1" selected="selected">{{ text_yes }}</option>
                                            <option value="0">{{ text_no }}</option>
                                        {% else %}
                                            <option value="1">{{ text_yes }}</option>
                                            <option value="0" selected="selected">{{ text_no }}</option>
                                        {% endif %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group required" id="div_payment_ul_card_dynamic_descriptor">
                                <label class="col-sm-2 control-label"
                                       for="payment_ul_card_dynamic_descriptor">{{ entry_dynamic_descriptor }}</label>
                                <div class="col-sm-10">
                                    <input type="text" class="form-control" id="payment_ul_card_dynamic_descriptor"
                                           name="payment_ul_card_dynamic_descriptor"
                                           value="{{ payment_ul_card_dynamic_descriptor }}"/>
                                </div>
                            </div>

                            <div class="form-group required">
                                <label class="col-sm-2 control-label"
                                       for="payment_ul_card_log_to_file">{{ entry_log_to_file }}</label>
                                <div class="col-sm-10">
                                    <select name="payment_ul_card_log_to_file" id="payment_ul_card_log_to_file"
                                            class="form-control">
                                        {% if payment_ul_card_log_to_file %}
                                            <option value="1" selected="selected">{{ text_no }}</option>
                                            <option value="0">{{ text_yes }}</option>
                                        {% else %}
                                            <option value="1">{{ text_no }}</option>
                                            <option value="0" selected="selected">{{ text_yes }}</option>
                                        {% endif %}
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div class="tab-pane" id="tab_order_status">
                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_new_status">{{ entry_new_payment_status }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_new_status"
                                            id="payment_ul_card_new_status"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_new_status == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_processing">{{ entry_payment_processing }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_processing"
                                            id="payment_ul_card_processing"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_processing == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_declined">{{ entry_payment_declined }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_declined"
                                            id="payment_ul_card_declined"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_declined == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_authorized">{{ entry_payment_authorized }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_authorized"
                                            id="payment_ul_card_authorized"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_authorized == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_completed">{{ entry_payment_completed }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_completed"
                                            id="payment_ul_card_completed"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_completed == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_cancelled">{{ entry_payment_cancelled }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_cancelled"
                                            id="payment_ul_card_cancelled"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_cancelled == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_charged_back">{{ entry_payment_charged_back }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_charged_back" id="payment_ul_card_charged_back"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_charged_back == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_chargeback_resolved">{{ entry_payment_chargeback_resolved }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_chargeback_resolved"
                                            id="payment_ul_card_chargeback_resolved"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_chargeback_resolved == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_refunded">{{ entry_payment_refunded }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_refunded" id="payment_ul_card_refunded"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_refunded == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_status_voided">{{ entry_payment_voided }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_voided" id="payment_ul_card_voided"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_voided == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-sm-3 control-label"
                                       for="payment_ul_card_terminated">{{ entry_payment_terminated }}</label>
                                <div class="col-sm-9">
                                    <select name="payment_ul_card_terminated" id="payment_ul_card_terminated"
                                            class="form-control">
                                        {% for status in order_statuses %}
                                            {% if payment_ul_card_terminated == status.order_status_id %}
                                                <option value="{{ status.order_status_id }}"
                                                        selected="selected">{{ status.name }}</option>
                                            {% else %}
                                                <option value="{{ status.order_status_id }}">{{ status.name }}</option>
                                            {% endif %}
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript" src="./view/javascript/ul/common_settings_ul.js"></script>
<script type="text/javascript" src="./view/javascript/ul/card/ul_settings_card.js"></script>
<script type="text/javascript" src="./view/javascript/ul/spinner.min.js"></script>
